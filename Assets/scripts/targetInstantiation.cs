using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class targetInstantiation : MonoBehaviour
{
    //public GameObject target;
    //public GameObject meteor1;
    //public GameObject meteor2;
    //public GameObject meteor3;
    //private Vector3 coordinates;
    //[SerializeField] private Transform meteorTransformPoint;
    //[SerializeField] private Transform targetTransformPoint;

    // Start is called before the first frame update
    void Start()
    {
        //targetTransformPoint.transform.position = new Vector3(meteorTransformPoint.transform.position.x, transform.position.y, meteorTransformPoint.transform.position.z);
       // Instantiate(target, targetTransformPoint.transform.position, Quaternion.identity);
    }

    // Update is called once per frame
    void Update()
    {
        
    }
    
}
